{
  "name": "server",
  "version": "1.0.0",
  "description": "REST API for cookie clicker",
  "main": "index.js",
  "scripts": {
    "server": "json-server --watch db.json --port 5000"
  },
  "author": "Filip",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
